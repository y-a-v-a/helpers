# Helpers

Helper tools

# shsh

Natural language to shell scripts.

# git-analytics.sh

Get insights in git repositories.

# update-projects.sh

Fetch from all remotes, runs garbage collect, assumes `~/Projects` directory with projects in subdirectories.

# chrome-no-cors

Start Chrome without network security (no CORS errors).

# algolia.sh

Script to see usage progress compared to contract limits.